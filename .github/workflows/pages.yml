name: Deploy latest RIFTWEAVE local build

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: riftweave-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Restore latest local Codex source
        run: |
          base64 -d latest-src.bundle.b64 > /tmp/rift-latest-src.tar.gz
          echo "89feb4cc8e3d02f87694d815f184cfbad30a7881f5395d5f9e4dab962e731ccc  /tmp/rift-latest-src.tar.gz" | sha256sum -c -
          tar -xzf /tmp/rift-latest-src.tar.gz -C .
      - run: npm ci
      - run: npm run build
      - run: npm test
      - run: node --check site-src/core.js && node --check site-src/board-view.js && node --check site-src/game.js
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: dist
      - uses: actions/deploy-pages@v4
        id: deployment
